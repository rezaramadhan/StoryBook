using UnityEngine;
using System.Collections;

public class SetPointActive : MonoBehaviour {
	public GameObject point;
	// Use this for initialization
	void Start () {
		point.SetActive (true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
